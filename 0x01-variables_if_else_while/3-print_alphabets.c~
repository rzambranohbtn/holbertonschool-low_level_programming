#include <stdio.h>
#include <ctype.h>
/**
*main - initial point
*Return: 0
*alphABET
*/
int main(void)
{
int x;
for (x = 'a'; x <= 'z'; x++)
putchar (x);
for(x = 'A'; x <= 'Z'; x++)
putchar(x);
putchar('\n');

return (0);
}
