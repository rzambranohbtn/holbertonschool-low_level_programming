#include <stdio.h>
/**
*main - initial point
*Return: 0
*alphabet game
*/
int main(void)
{
int x;
do {
(x = 'a'; x <= 'z'; x++)
for (x != 'q'; x != 'e'; x++)
putchar (x);
putchar('\n');

return (0);
}
