#include "holberton.h"
/**
 *print_square - square
 *@size: entry
 */
void print_square(int size)
{
	int x;
	int y;

	for (x = 0; x <= size; x++)
	{
		for (y = 1; y < size; y++)
		{
			_putchar('#');
			if (size == 0)
			_putchar('\n');
		}
		_putchar('\n');
	}
}
